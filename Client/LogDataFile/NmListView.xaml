<UserControl x:Class="OpenWLS.Client.LogDataFile.NmListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <ListView  x:Name ="chLV"  MouseDoubleClick="chLV_MouseDoubleClick" >
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" Width="120" DisplayMemberBinding="{Binding Name}" />
                    <GridViewColumn Header="Type" Width="120" DisplayMemberBinding="{Binding RecordType }"/>
                    <!--GridViewColumn Header="Size" Width="40" DisplayMemberBinding="{Binding Size  }"/-->
                </GridView>

            </ListView.View>

        </ListView>

    </Grid>
</UserControl>
